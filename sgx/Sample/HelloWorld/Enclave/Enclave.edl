/* Enclave.edl - Top EDL file. */

enclave {
    
    trusted{
        public void my_helloworld();
	};
    untrusted {
        size_t ocall_write(int fd, [in, size=count] const void *buf, size_t count);
    };

};
