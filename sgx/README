1. You need to build your lib_pypyc.a using the pypy source code we provide. Go into pypy/goal,
        Run ../../rpython/bin/rpython --opt=2 targetstandaloned.py --no-allworkingmodules
   wait until your libpypy_c.a is generated. While you can set path for libpypy_c.a or you can find it in the tmp directory whil
e checking the ternimal display.

2. Put the libpypy_c.a in the directory useesion in your Enclave folder.

3. Build the python's c interface.

NOTICE: While running pypy embed.py, make sure the typedefine.h and include directory are in the same directory as the command running, and import the cffi module from the lib_pypy we provide.

